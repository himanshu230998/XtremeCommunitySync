name: DEV Deploy

env:
  value: xyzqwerty
  # JSON: ${{ toJSON(github) }}

on:
  workflow_dispatch:
  push:
    branches: 
      - '**'

jobs:

  
  Dags:
    name: 'Dags/Sync'

    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

        
      - name: sync to s3
        run: |
          echo dev branch name ${{ github.event.ref }}
          echo github "${{ env.JSON }}" || true
          value="xyzqwerty"

  docker01:
    name: "check variable exists"
    if: contains('${{ env.value }}', 'xyz')
    runs-on: ubuntu-22.04
    steps:
      - name: haaaaaaha
        run: |
          echo done